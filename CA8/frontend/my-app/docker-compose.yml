version: "2"
services:
  nginx-react:
    container_name: ngixreactapp
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - 9000:80
    environment:
      NODE_ENV: production